# Read user input
[print(x) for x in input().split() if len(x) % 2 == 0]
# even_length = [x for x in input().split() if len(x) % 2 == 0]

# Logic
# for el in even_length:
#     print(el)
# End of Logic

# Print Output
# print('\n'.join(even_length))
